The first parameter of a multi-line function declaration must be on the line after the opening
Multi-line function declarations must define one parameter per line
Multi-line function declaration not indented correctly; expected
Multi-line function call not indented correctly; expecte
The closing parenthesis of a multi-line function declaration must be on a new line
Only one argument is allowed per line in a multi-line function call
Closing parenthesis of a multi-line function call must be on a line by itself
Opening parenthesis of a multi-line function call must be the last content on the line
The closing parenthesis of a multi-line control structure must be on the line after the last
The first expression of a multi-line control structure must be on the line after the opening
The closing parenthesis and the opening brace of a multi-line function declaration must be on the